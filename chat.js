// Перевірити чи є користувач преміум підписником
function checkPremiumStatus(userId) {
    // Тут можна додати логіку для перевірки статусу користувача (наприклад, звернення до бази даних або API)
    // Для прикладу, припустимо, що користувач з ID 1 є преміум підписником
    return userId === 1;
}               
// Функція для відображення контенту
function displayContent(userId) {  
    if (checkPremiumStatus(userId)) {
        // Відобразити преміум контент
    } else {
        // Відобразити стандартний контент
    }
}
// Приклад використання
const userId = 1;            
displayContent(userId);